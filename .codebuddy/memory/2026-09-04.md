# 2026-09-04

## M2 Phase 4B Local Runner —— 四轮 review 收敛

- **状态**：M2 代码完成、全量门禁有效通过（`pnpm test` 1172 通过 / 16 跳过 / 0 失败，EXIT=0；typecheck 25/25）。**仍未验收**——缺目标平台证据。
- **完成**：`be4b3fb...HEAD` 三轮修复（提交 2156172→9f4ad01），闭合了二轮 review 的 4 个 P0 与全部 P1，三轮 review 无 P0。
- **根因修复（全量测试挂起）**：`docker-contract-suite.test.ts` 的 `spawnSync("docker", ["info"])` 无超时，Docker Desktop 装了但 daemon 没跑时在 collection 阶段无限阻塞。既有 `docker-environment.test.ts` 早有 `timeout: 5_000` 防御，新套件漏抄。修复后本机 daemon 时好时坏，故套件改为 `LECODING_DOCKER_CONTRACT=1` 显式 gate。
- **评审方法论教训**：贯通测试（四层真实串联）是关键——单测各自通过但协议载荷不兼容，四层连起来才暴露。第二轮发现的 4 个 P0 中 3 个属于"值被产生但从未消费"（recover 结果丢弃、auditStatus 无调用方、interrupted 未进协议层去重）。
- **修复时引入过的新缺陷**（review 才发现）：Seatbelt wrapper 返回 Promise 未 await 导致内核强制静默失效；`ws` 客户端默认开 permessage-deflate 违反计划要求；测试用 `globalThis.WebSocket`（Node 20 没有）导致 3 个测试假通过。
- **遗留（已声明边界，见 docs/m2-completion-summary.md §5.2）**：Windows 无内核级 FS 限制（argv_fence best-effort，需原生 AppContainer）；PolicyEngine `fileAccessScope` 未参与决策（建议删除）；项目声明验证命令本地未消费；组织策略禁用 host_full 未实现。
- **下一步**：目标平台证据归档（docs/evidence/desktop-m2-smoke-checklist.md，需真实 macOS/Windows）→ M3。

## 环境注意
- 本机 Docker daemon 不稳定，`docker info` 会无限阻塞——任何 Docker 探针必须带 timeout。
- 全量 `pnpm test` 约 88s；后台跑要 `nohup bash script.sh & disown`（macOS 无 setsid；`pgrep -f 脚本名` 会误报 FINISHED，用 `pgrep -f "vitest run"` 判断）。
