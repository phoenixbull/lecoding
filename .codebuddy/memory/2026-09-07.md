# 2026-09-07

## M3 按「本机可做 / 外部待验收」拆分推进（第一轮）

- **执行依据**：`docs/m3-implementation-plan.md`（用户指定）+ 用户确认的顺序：字段清理 → retention/cleanup/residual 验证 → 文档收口。
- **结论**：M3 **未达发布判定**，只达 RC 状态。详见新增的 `docs/m3-completion-summary.md`。

### 已完成（3 个提交）
1. `3ff3ae0` **M3.A1 部分**：删除 `CapabilityRequest.fileAccessScope`（从未参与决策的无效输入，保留会制造虚假安全承诺）。从请求类型、RunEngine 两处 `authorize` 调用、所有测试中移除；`environment.prepare` 的 scope **保留**（环境真实消费）。删除是类型强制的 + 3 条测试钉边界。固定 deny 与 Run 级 deny list 不动。
2. `c8ead07` **M3.C 部分**：retention worker 新增 9 条确定性测试（七日边界、干净运行仍上报、residual path、失败路由 onError 且不剪除、并发合并、stop 后拒绝重启、重复 stop、间隔下限）。**明确声明不构成真实 PostgreSQL 证据**（`pruneExpired` 无内存实现）。cleanup/residual 告警此前已覆盖。
3. `ca67614` **M3.D 部分**：新增 `docs/m3-completion-summary.md`（环境要求/命令/验收标准/待归档证据/已知限制/逐项发布检查清单）；更新 `latest-development-plan.md` M3 章节与里程碑表；README 增加 Desktop and Local Runner 与平台限制章节（原 workspace 仍写 apps/ 为 future）。

### M3.0 CI 根因（重要）
- 远端 CI 在 `c33be0b` 失败：`packages/runner-protocol/test/session.test.ts` 缺 `RunnerCommandOutcome` 导入（2 处），退出码 2。
- **本地为何未暴露**：`pnpm typecheck` 命中 turbo 缓存。**教训：验收必须用 `pnpm exec turbo run typecheck --force`（零缓存），普通 `pnpm typecheck` 会掩盖真实失败。**
- 修复已在未推送的 `9f4ad01`；「同 SHA CI 全绿」需推送后复核——推送未执行（需用户确认）。
- 诊断获取方式：`web_fetch` 抓取 GitHub Actions run URL（公共仓库可读 annotations，含具体 TS 诊断）。

### 环境现状（M3 外部项全部阻塞）
- 无 `ANTHROPIC_API_KEY`；无真实 PostgreSQL（无 psql/pg_ctl，无 `LECODING_DATABASE_URL`）；Docker daemon 无限阻塞（`docker info` exit 124）；无签名证书；无目标平台机器。
- 本机全量门禁：**1184 通过 / 16 跳过 / 0 失败，EXIT=0**；typecheck 25/25。

### 待办（下一轮）
- M3.A2 更新生产链路：`installVerifiedUpdate` 调用点仅见测试，生产 Main 未接线；缺签名更新清单生产链路。
- M3.B 全部外部证据；M3.C 备份恢复与 schema 升级/回滚演练（需独立 staging 数据库）。
- M3.D 剩余：Phase 4 audit 更新、证据索引 `docs/evidence/m3/<candidate>/`、release notes 定稿。
- 需用户确认：12/12 黄金任务通过率是否作为新的发布准入标准。
