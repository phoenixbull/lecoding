# 2026-09-07

## M3 按「本机可做 / 外部待验收」拆分推进

- **执行依据**：`docs/m3-implementation-plan.md`（用户指定）+ 用户确认顺序：字段清理 → retention/cleanup/residual → 文档收口；其后继续 M3.A2。
- **结论**：M3 **未达发布判定**，只达 RC。详见 `docs/m3-completion-summary.md`。

### 提交记录（5 个）
1. `3ff3ae0` M3.A1：删除 `CapabilityRequest.fileAccessScope`（从未参与决策）。保留 `environment.prepare` 的 scope（环境真实消费）。类型强制删除 + 3 条边界测试。
2. `c8ead07` M3.C：retention worker 1→10 条测试。**明确声明不构成真实 PostgreSQL 证据**（`pruneExpired` 无内存实现）。cleanup/residual 此前已覆盖。
3. `ca67614` M3.D：新增 `docs/m3-completion-summary.md`（环境要求/命令/验收标准/待归档证据/已知限制/发布检查清单）；更新 plan 与 README（原 workspace 仍写 apps/ 为 future）。
4. `ac5eadd` **M3.A2**：更新生产链路接线。新增 `buildSignedUpdatePackage`（确定性序列化 + SHA-256 + Ed25519）、`createAutoUpdater`、`createDesktopUpdater`/`loadUpdaterConfig`/`runPlatformInstaller`（新文件 `apps/desktop/src/main/updater.ts`），并在 `electron.ts` 装配根接入。测试 16 条覆盖全部拒绝路径。
5. `055e03f` 文档同步 A2 状态。

### M3.0 CI 根因（重要方法论）
- 远端 CI 在 `c33be0b` 失败：`packages/runner-protocol/test/session.test.ts` 缺 `RunnerCommandOutcome` 导入，退出码 2。
- **本地为何未暴露**：`pnpm typecheck` 命中 turbo 缓存。**验收必须用 `pnpm exec turbo run typecheck --force`（零缓存）。**
- 修复在未推送的 `9f4ad01`；「同 SHA CI 全绿」需推送后复核（推送未执行）。
- 诊断方法：`web_fetch` 抓 GitHub Actions run URL，公共仓库可读 annotations 里的具体 TS 诊断。

### 环境现状（M3 外部项阻塞）
- 无 `ANTHROPIC_API_KEY`；无真实 PostgreSQL；Docker daemon 无限阻塞；无签名证书；无目标平台机器。
- 本机门禁：**1200 通过 / 16 跳过 / 0 失败，EXIT=0**；零缓存 typecheck 25/25。

### 坑
- zsh 会解释 `node -e "..."` 里的反引号 → 写脚本文件到 /tmp 再执行。
- vitest 无全局，测试须显式 import。
- A2 里 `installVerifiedUpdate` 是「有测试、无调用者」的典型：Gate 存在不等于生效，接线前生产无法安装也无法拒绝更新。

### 待办
- M3.B 全部外部证据；M3.C 备份恢复与 schema 升级/回滚演练（需独立 staging 数据库）。
- M3.D 剩余：Phase 4 audit 更新、`docs/evidence/m3/<candidate>/` 证据索引、release notes 定稿。
- 需用户确认：是否推送验证 CI；12/12 黄金任务通过率是否作为发布准入标准。
