# 2026-09-07

## M3 按「本机可做 / 外部待验收」拆分推进

- **执行依据**：`docs/m3-implementation-plan.md`（用户指定）；顺序：字段清理 → retention/cleanup/residual → 文档收口 → M3.A2 → 文档草稿。详见 `docs/m3-completion-summary.md`。
- **结论**：M3 **未达发布判定**，只达 RC。用户明确：未取得证据的项目继续标记「待目标环境」，M3 暂不关闭。
- **用户确认的剩余顺序**：完成本机文档草稿 → 推送候选并通过 CI → M3.B 外部验证 → M3.C staging 演练 → 按证据定稿 M3.D。

## 提交（本轮 7 个）
1. `3ff3ae0` M3.A1 删除 `CapabilityRequest.fileAccessScope`（保留 `environment.prepare` 的 scope）。
2. `c8ead07` M3.C retention worker 1→10 条测试（声明不构成真实 PostgreSQL 证据）。
3. `ca67614` M3.D 初版：m3-completion-summary + plan + README 平台限制。
4. `ac5eadd` **M3.A2**：更新生产链路接线（新文件 `apps/desktop/src/main/updater.ts`）。
5. `055e03f` 文档同步 A2。
6. `d64fbf2` **12/12 准入标准 + 文档草稿收口**：`docs/evidence/m3/README.md`、release notes 草稿（§6.1）、Phase 4 audit 重写门禁段、runbook 兼容矩阵（R1.5）。

## 关键决策（用户确认，须遵守）
- **12/12 黄金任务全部通过** = 本次 RC 准入标准。三条限定：①保留全部轮次 ②失败即修复并重跑完整集合，禁止拼接多轮成功结果 ③12/12 只证明该验收集通过，**不代表生产成功率 100%**。现有 live 测试不断言通过率（只断言任务数与 token 非零），需在公开评估结果 seam 上加报告门禁。
- **推送**：用户建议推送验证同 SHA CI 全绿，但说明「不能替我授权」。**未执行推送**——需用户明确指令后才可 push。当前本地领先远端 6 个提交。

## M3.0 CI 根因（方法论）
- `c33be0b` 失败：`packages/runner-protocol/test/session.test.ts` 缺 `RunnerCommandOutcome` 导入（2 处），退出码 2。
- **本地未暴露的原因**：`pnpm typecheck` 命中 turbo 缓存。**验收必须 `pnpm exec turbo run typecheck --force`（零缓存）**——已写入 Phase 4 audit。
- 诊断方法：`web_fetch` 抓 GitHub Actions run URL，公共仓库可读 annotations 里的具体 TS 诊断。

## 环境现状（外部项阻塞）
无 `ANTHROPIC_API_KEY`；无真实 PostgreSQL（无 psql/pg_ctl）；Docker daemon 无限阻塞；无签名证书；无目标平台机器。
本机门禁：**1200 通过 / 16 跳过 / 0 失败，EXIT=0**；零缓存 typecheck 25/25。

## 坑
- zsh 会解释 `node -e "..."` 里的反引号 → 写脚本文件到 /tmp 再执行。
- vitest 无全局，测试须显式 import。
- 两类「假完成」在本项目反复出现，评审时重点查：①Gate 存在但无调用者（如 `installVerifiedUpdate`）②值被产生但从未消费（recover 结果、auditStatus、seedCommands）。

## 待办
- 推送候选并验证 CI（待用户授权）。
- M3.B：Linux 隔离、Docker 契约、PostgreSQL smoke/并发、Anthropic 黄金任务、签名公证、三平台 smoke。
- M3.C：备份恢复与 schema 升级/回滚演练（独立 staging 数据库）。
- M3.D：证据按候选 SHA 归档、release notes 定稿。
