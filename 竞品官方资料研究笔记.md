# Claude Code / Codex 官方能力对标笔记

> 核验日期：2026-08-18  
> 范围：仅采用 Anthropic、OpenAI 官方文档和官方开源仓库。产品迭代较快，实施前应再次确认。

## 核心事实

| 维度 | Claude Code | OpenAI Codex | 对本项目的启示 |
|---|---|---|---|
| 基本执行闭环 | 读取代码、编辑文件、运行命令并验证；覆盖终端、IDE、桌面和 Web | CLI 可检查、编辑、运行代码；另有 IDE、桌面与云端形态 | ReAct 只是基础，竞争力主要来自可靠的执行 Harness |
| 项目指令 | `CLAUDE.md`、路径规则及组织/用户/项目作用域 | 分层 `AGENTS.md` / `AGENTS.override.md`，支持目录级覆盖与大小上限 | 先做显式、可审计、按项目/目录作用域的指令，不要先做全局 Prompt 自动进化 |
| 跨会话记忆 | 自动记忆为可查看、编辑、删除的 Markdown；按仓库隔离，有加载上限 | 支持 memories，并通过项目指令与 skills 保存稳定流程 | 记忆应有来源、作用域、上限、人工审计和删除能力；向量库不是 MVP 必需品 |
| 权限 | allow / ask / deny 规则；多种权限模式；规则由 Harness 强制，不由模型决定 | 沙箱模式、审批策略、细粒度附加权限、可持久化执行策略 | “危险命令黑名单”不够，应使用默认拒绝、能力分级和最小权限审批 |
| 沙箱 | 文件系统与网络均有 OS 级约束；网络通过沙箱外代理做域名控制和审计 | read-only / workspace-write / full-access 等模式；命令审批与沙箱联动 | 需要可写工作区 + 独立只读基线；网络应用代理/白名单，而非永久 `network=none` |
| 隔离并发 | 子 Agent 可使用独立 Git worktree，并检查命令不逃逸到主检出目录 | 桌面端以 worktree 运行并行任务，避免互相干扰 | “每会话一个容器”不能代替代码版本隔离；每个 run 应有独立 worktree/snapshot |
| 扩展机制 | MCP、skills、hooks；`PreToolUse` 可在所有权限模式前阻断操作 | MCP、skills、plugins、hooks，以及 SDK/App Server | 工具注册表应预留统一扩展 seam；策略钩子应独立于 Agent prompt |
| 多 Agent | 支持子 Agent、Agent teams、后台并行任务 | 支持 subagents，CLI 可拆分复杂调查 | MVP 延后多 Agent 合理，但状态模型应预留父子 run 与隔离 workspace |
| 会话生命周期 | 支持 resume、fork/compact 相关生命周期事件 | CLI 支持 resume；SDK/App Server 有 thread start/resume/fork、turn interrupt/steer 和事件流 | SSE 只是展示通道；任务必须由可恢复的后台状态机承载 |
| Git/交付 | 可建分支、提交和创建 PR，提供可视化 diff | worktree、review、GitHub/CI 集成 | Diff、恢复点、提交/PR 是核心工作流，不应长期排在产品后面 |

## 设计层面的推断

以下为基于官方能力的工程推断，不是厂商公开的内部实现：

1. 成熟 Coding Agent 的核心不是显式拆成 `Planner + Reflector`，而是一个可恢复、可审批、可验证、可审计的运行时。
2. 稳定规则与经验更适合作为用户可编辑的项目文件或版本化记录；直接自动追加系统提示词会放大错误经验和提示注入。
3. 并发任务既需要进程/容器隔离，也需要 Git/workspace 隔离，否则同一仓库中的修改会互相覆盖。
4. “模型宣布 finish”不能等价于成功；成功应来自测试、类型检查、lint、用户验收条件和最终 diff 等证据。

## 官方来源

### Anthropic

- [Claude Code Overview](https://code.claude.com/docs/en/overview)
- [Configure permissions](https://code.claude.com/docs/en/permissions)
- [Configure the sandboxed Bash tool](https://code.claude.com/docs/en/sandboxing)
- [How Claude remembers your project](https://code.claude.com/docs/en/memory)
- [Create custom subagents](https://code.claude.com/docs/en/sub-agents)
- [Automate actions with hooks](https://code.claude.com/docs/en/hooks-guide)

### OpenAI

- [Codex CLI](https://learn.chatgpt.com/docs/codex/cli)
- [AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [Git worktrees](https://learn.chatgpt.com/docs/environments/git-worktrees)
- [Build skills](https://learn.chatgpt.com/docs/build-skills)
- [Codex App Server protocol and approvals](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md)
- [Codex Python SDK thread lifecycle and sandbox presets](https://github.com/openai/codex/blob/main/sdk/python/docs/api-reference.md)
- [Codex open-source repository](https://github.com/openai/codex)
