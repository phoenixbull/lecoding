# Phase 0 Status

Updated: 2026-08-27

## Evidence completed

- pnpm workspace, Turborepo, TypeScript strict mode, and Vitest configured.
- RunEngine, RunEnvironment, Verifier, PolicyEngine, and Workspace interfaces created.
- Run success is gated by a VerificationReport.
- Docker socket hard deny is enforced independently of approval mode.
- A real temporary Git repository test proves worktree isolation.
- Client SDK uses the versioned `/api/v1` Run endpoint.
- RunEvent envelopes reject unsupported protocol versions.
- A FakeModel tracer executes a structured command tool call before verification.
- Manual Runs pause with a client-readable approval request and resume after one-time approval.
- A rejected tool call is returned to the model without execution.
- Agent-loop failures become queryable failed Runs instead of remaining active.
- Environment errors during command execution are parked safely: when `perform` throws, the run is moved to `environment_offline` and the local handle is released. The exact pending tool call remains durable; after re-prepare, a completed ledger entry is recovered without replay, while an unfinished entry becomes `tool_call_outcome_unknown` instead of automatically repeating a possibly partial side effect.
- Environment errors during environment preparation are auto-recovered the same way: when `prepare` throws, the run is moved to `environment_offline` (without needing to dispose since no handle has been registered yet), so a worker reporting image-pull failures or unreachable workspaces parks the run for re-preparation instead of throwing back to the scheduler.
- `LeaseHeartbeat` seam keeps the lease alive across long awaits: `prepare` (which can spend minutes pulling images) is wrapped in `heartbeat.withHeartbeat(token, leaseMilliseconds/2, fn)`, so a worker holding the lease auto-renews on a half-interval cadence and is not preempted by another worker that picked up an expired lease from the same run id.
- `LeaseHeartbeat` covers the verification path too: `environment.inspect`, `verifier.verify`, and terminal runtime cleanup are wrapped in the same heartbeat wrapper, so the lease survives a verification or evidence-preserving container disposal that takes longer than a single lease period. Cancel/recover disposal remains immediate on its command-owned path.
- `LeaseHeartbeat` actively invalidates the lease when renewal fails: the wrapper calls `lease.invalidate` as soon as a renewal returns false, so a worker that lost its network path to the lease backend (PG, etc.) immediately releases the lease instead of waiting for natural expiry. This gives other workers a takeover path that does not depend on the dying worker keeping a tick.
- PostgreSQL-backed `RunLease` with generation-based optimistic locking: `createPostgresRunLease` uses a `run_engine_leases` table and a monotonically increasing `generation` column. `acquire` atomically inserts-or-updates only when the existing lease has expired, `renew` requires both `owner_id` and `generation` to match, and `invalidate` bumps `generation` so a stale token can never renew again — solving the cross-process invalidate-delay problem without depending on wall-clock expiry. Exercised by an embedded PGlite integration test (7 cases covering acquire contention, renew-by-owner, invalidation, stale-token renew, and release).
- Cross-worker cancel via PG LISTEN/NOTIFY with a heartbeat-based loss fallback: `createPostgresRunCancelBus` backgrounds a `LISTEN run_engine_cancel` session, `publish` fires `pg_notify`, and the cancel path additionally marks the run `cancelled` in the shared store. If the NOTIFY broadcast is ever lost (session disconnect, cross-server isolation), `performWith`'s per-tick `tickCancellationFallback` reads the store and aborts the handle once the terminal state is visible, so cancel degrades from "broadcast" to "eventual-consistency store check" rather than being dropped. Failover reconnects with exponential backoff and re-registers the LISTEN. Covered by two complementary integration matrices with PGlite: (a) single-instance multi-session fanout (equivalent to many workers on one PG server), and (b) multi-instance isolation (real cross-server boundary where NOTIFY provably does not cross) proving the store+heartbeat path aborts a hung perform and disposes its environment.
- Production recovery now uses pinned pg-boss 10.4.2 through the existing `RunRecoveryWorker` seam. A short-policy scan queue durably chains its next `startAfter` job, filters expired leases to non-terminal Run snapshots, and emits singleton-keyed per-Run jobs with bounded exponential retry. pg-boss supplies PostgreSQL SKIP LOCKED claiming across Workers; RunEngine's generation lease remains the final state-transition authority. PGlite integration proves real pg-boss schema migration, queue creation, claiming, terminal exclusion, callback delivery, and graceful shutdown. The interval adapter remains only for deterministic lease takeover tests.
- Run-scoped approval/rejection policy and durable continuation recovery: each Run can declare `deniedCommands` at start time; commands matching the list are denied before any global rule, so a Run can opt out of `curl`/`docker` even when `approvalMode` is `full_access`. Paired with a continuation-recovery guard at the top of `drive` that refuses to re-invoke the model when the Run is in `waiting_approval` — a worker that resumes a run left waiting for approval now leaves it waiting (the persisted `pendingApproval` is the continuation point), instead of restarting the model loop and producing a duplicate tool call.
- Docker runtime limits + cancellation PoC: `RunEnvironment.perform` accepts an `AbortSignal`, the `RunHandleRegistry` keeps an `AbortController` per registered handle, and the `cancel` command invokes `handles.abort(runId)` before its `cancelling` transition so a long-running `perform` is interrupted immediately rather than waiting for natural completion. `createDockerRunEnvironment` executes the fixed security plan and applies `execTimeoutMs` across `docker run`/`exec`/`kill`; `FakeDockerRunEnvironment` retains deterministic OOM/cgroup-failure coverage so environment errors transition the Run to `environment_offline`.
- `createDockerRunPlan` makes the container creation contract independently auditable and non-downgradable: numeric uid/gid 10001, read-only rootfs, `cap-drop=ALL`, `no-new-privileges`, network none, positive CPU/memory/PID/nofile limits, bounded noexec/nosuid/nodev tmpfs, and exactly one workspace mount below the registered worktree root. `prepare` resolves both paths through filesystem `realpath`, so lexical and symlink mount escapes are rejected before Docker is invoked; unbounded memory configurations are rejected too.
- Docker Desktop 27.5.1 live PoC now executes rather than permanently skipping: a real Alpine container is observed through `RunEnvironment` as uid 10001, unable to write `/etc`, able to write only the worktree and `/tmp`, and an in-flight `sleep 30` is interrupted by AbortSignal/docker kill. This is macOS development evidence; target-Linux isolation evidence remains required.
- Runs waiting for approval can be cancelled and their environment discarded.
- Cancellation racing a pending model call keeps the Run cancelled: the driver loop re-checks state at the model-call boundary and never executes the cancelled tool call.
- Cancellation racing an in-flight command keeps the Run cancelled: RunStore saves are guarded by an optimistic version, stale writes fail with RunConflictError, and a superseded loop never overwrites the concurrent terminal state.
- RunEngine exposes a separated `start` / `resume` seam: `start` only enqueues a `queued` Run and returns; Worker callers drive execution by `resume(runId)`, which is idempotent across terminal states and re-takeover scenarios.
- EnvironmentHandle lives in a process-local RunHandleRegistry: persistence never stores runtime resources, so process restarts no longer hold zombie handles.
- `recover_environment` command transitions a run to `environment_offline`, disposes the registered environment, and releases the handle; the next `resume()` re-prepares the environment and continues execution so a worker can drop its environment without losing the run.
- `recover_environment` is a no-op for terminal or non-driver-startable runs: the gate keeps an already `succeeded`/`failed`/`cancelled` run from being pulled back to `environment_offline`, so a late recovery request can never resurrect a finished run or dispose its finished environment.
- Workers can trigger environment recovery internally without the user command interface: `RunResumer.recoverEnvironment(runId)` acquires the lease on its own and reuses the same `environment_offline` transition, so a worker-side fault self-check can drop a broken environment while its drive loop parks safely.
- `RunLease` seam prevents concurrent Worker takeover: every `resume()` and `command()` acquires a per-run lease through `acquire`/`renew`/`release`/`invalidate`; the drive loop renews the lease at each await boundary (model call and tool execution) so a worker that loses its lease stops driving and never overwrites a new owner's state.
- `RetryPolicy` seam queues `resume()` lease acquisition: when another worker holds the lease, `acquireLease` waits according to the injected policy (default 5s budget with exponential backoff capped at 200ms) instead of giving up immediately. `command()` does not retry because user-initiated commands need an immediate owner decision.
- Every state write is guarded at the `transition` boundary: a worker holding a lease token re-verifies it before persisting any status change, and a lost lease fails the write with `LeaseLostError` instead of silently skipping, so a superseded owner can never overwrite a new owner's state or mark the Run as `agent_loop_failed`.
- Tool-result writes are guarded the same way: `performWith` re-verifies the lease after the command finishes but before persisting its result, so a worker that loses its lease during command execution discards the observed result instead of polluting the next owner's model input.
- RunEvent V1 strictly validates every envelope field and JSON-safe payload data.
- The in-memory RunEventJournal assigns monotonic per-Run sequences and resumes after `Last-Event-ID`.
- RunEngine publishes ordered lifecycle events for Web and future PC clients. Approval requests and Agent questions are committed with their waiting snapshots; tool-start markers prevent duplicate start evidence after Worker takeover; tool results and completion events share one write; verification/failure evidence is inserted before its terminal status delimiter. Tool events expose only the executable name, argument count, outcome, and exit code—not raw argv or command output.
- Client SDK opens fetch-based SSE streams with a resumable event cursor.
- Client SDK now owns the complete minimum control-plane surface: bootstrap config, bounded project history, Run creation, inspection, strict fragmented SSE decoding, cancellation, single-call approval/rejection, persisted user answers, and live steer commands.
- PostgreSQL atomically allocates event sequences and inserts matching outbox records.
- Outbox workers use leased `SKIP LOCKED` claims and owner-only acknowledgements.
- The outbox dispatcher provides explicit at-least-once delivery and acknowledges only completed batches.
- The long-lived SSE handler replays backlog before ordered live events without a subscription race.
- SSE clients deduplicate repeated outbox deliveries by stable per-Run sequence.
- RunEvent V1 includes durable conversation lifecycle events for submitted user input, delivery at a safe model boundary, and Agent questions. The Web timeline renders these as inert text and replays historical Runs through their terminal status event.
- Live steer commands now carry a client-stable idempotency key. A single PostgreSQL CTE commits the mailbox message, `user_message_submitted` event, and delivery outbox together; exact retries reuse the first message sequence, while an ID/content mismatch fails closed. The Client SDK generates a UUID by default and permits callers to reuse an explicit ID after an uncertain response.
- Answer commands now use the same client-stable identity and persist a durable Run receipt. Resolving `waiting_user` atomically consumes its question, appends the provider tool result, advances status, records the receipt, and inserts submitted/delivered events plus outbox rows. The generalized transition writer also commits the steering cursor snapshot with `user_message_delivered`, eliminating the remaining delivery-confirmation crash window.
- PostgreSQL behavior is exercised by an embedded PostgreSQL-compatible integration test.
- PostgreSQL-backed `RunStore` persists the complete Run continuation snapshot and applies compare-and-swap version checks across adapter instances, so a replacement Worker reads the latest durable state and stale writers cannot overwrite it.
- PostgreSQL-backed `ToolCallLedger` claims `(run_id, call_id)` before execution and records the result immediately after the environment returns. A completed result is replayed into the Run snapshot without executing the tool again; an unfinished claim is surfaced as `tool_call_outcome_unknown` and is never automatically replayed.
- Every model-issued tool call is now persisted in `pendingToolCall` before policy evaluation or environment execution. Worker replacement resumes that exact `call_id` and provider continuation without calling the model again; consuming the pending call and appending its tool result occur in the same optimistic Run snapshot save.
- The side-effect crash window has a fault-injection test: Worker A executes the command and loses persistence before recording completion; Worker B observes the durable unfinished claim, performs zero additional commands, and fails the Run safely for reconciliation.
- `PostgresRunTransitionWriter` atomically applies the Run snapshot CAS, allocates the next event sequence, inserts the `status_changed` event, and queues its outbox record in one SQL statement. RunEngine delegates transition ownership to this seam when injected; a stale snapshot produces neither a state write nor an orphan event.
- A deterministic golden evaluation catalog now contains 20 initially failing, dependency-free repository tasks across bug-fix, feature, security, documentation, and performance categories. The stable five-task representative set covers one category each and includes both Node and Python fixtures; every task carries explicit acceptance criteria and shell-free verification argv.
- `createCodexExecGoldenTaskExecutor` materializes every task as a fresh Git repository, invokes non-interactive Codex with workspace-only writes and approvals disabled, parses JSONL token usage, calculates cost from an explicit model pricing snapshot, and independently executes the fixture verification commands. Missing usage fails closed so an unverifiable invocation cannot be recorded as a zero-cost success.
- `createOpenAiResponsesAgentModel` maps the Responses API into the existing `AgentModel` seam with strict `execute_command` and `request_user_input` function schemas and parallel tool calls disabled. Provider output is treated as untrusted: incomplete responses, unknown tools, multiple calls, missing call IDs, malformed JSON, invalid argv, and malformed questions all fail before PolicyEngine or RunEnvironment can see an action.
- Responses continuation is now durable Run state: every tool-call turn may carry the provider `response.id`, RunEngine persists it alongside the executed or denied tool result, and the next model request uses it as `previous_response_id` with a matching `function_call_output`. A composition test drives the Responses adapter through RunEngine command execution and Verifier success.
- `createOpenAiResponsesClient` provides the HTTP transport boundary, defaulting to the official HTTPS endpoint, with Bearer authentication, a bounded request timeout, configurable compatible endpoint, and status-only provider failures so credentials and remote error bodies do not enter Run failure text.
- Provider selection is no longer tied to OpenAI credentials or model names: `loadOpenAiCompatibleModelConfig` reads the neutral `LECODING_MODEL_PROTOCOL`, `LECODING_MODEL_BASE_URL`, `LECODING_MODEL_API_KEY`, and `LECODING_MODEL_ID` settings, while `createOpenAiCompatibleAgentModel` composes them into the existing gateway. Remote plaintext endpoints are rejected before credentials can be sent; HTTP remains available only for loopback development servers.
- Providers exposing only the OpenAI Chat Completions contract are now supported through `openai_chat_completions`. The adapter maps the same strict `execute_command` seam to chat tool calls, persists the stateless assistant/tool message history as the Run continuation, validates that history and its call ID before reuse, and rejects corrupted continuation state before contacting the provider.
- A live two-turn smoke test against the configured third-party provider completed on 2026-08-25: the first Chat Completions request produced one valid `execute_command` call and durable continuation, and the second request accepted the synthetic command result and returned a completed assistant turn. No provider secret, model response text, or model identifier was captured in the evidence.
- Chat Completions provider batches are serialized even when an upstream ignores `parallel_tool_calls: false`: the versioned continuation envelope persists the active call and remaining queue, RunEngine observes one call at a time, and the provider is contacted again only after every result in the batch is present. Duplicate call IDs, malformed queues, and mismatched result IDs fail closed.
- Responses and Chat Completions now replay an identical model request at most once when its HTTP body or tool arguments are syntactically invalid JSON. No turn reaches PolicyEngine or RunEnvironment before parsing succeeds; schema-invalid output is never retried, retry exhaustion preserves the original stable failure, and every parseable response is included in token telemetry even when its malformed tool arguments trigger the replay.
- The executable Worker logs each malformed-JSON retry chain as a fixed structured projection: Run ID, public protocol name, retry count, stable `http_body_invalid_json` or `tool_arguments_invalid_json` category, and `retrying` followed by `recovered` or `exhausted`. The formatter explicitly selects those fields instead of spreading callback input, and excludes model/endpoint identity, provider text, API keys, and command arguments. Observer failures cannot change the model turn.
- The five representative golden tasks completed 5/5 through the configured third-party model and Docker-isolated execution path. The run observed 58,911 input tokens, 9,073 output tokens, and 93.039 seconds total duration; the `$0.010788` cost is an official pay-as-you-go list-price equivalent, not the actual personal Token Plan charge. Full evidence is recorded in [`evidence/golden-baseline-2026-08-25.md`](evidence/golden-baseline-2026-08-25.md).
- The category-balanced 12/20 golden acceptance suite ran twice before malformed-JSON retry hardening and passed 10/12 then 11/12; all three failures were compatible-provider HTTP/tool JSON failures before verification. A third post-change run passed 12/12 through fresh Git fixtures and isolated Docker verification. The report does not expose retry-count telemetry, so it proves the hardened end-to-end path passed this run without claiming that a replay occurred. Full evidence and all retained source reports are recorded in [`evidence/golden-acceptance-2026-08-27.md`](evidence/golden-acceptance-2026-08-27.md).
- The production Worker composition root now initializes PostgreSQL Run storage, atomic transitions/events, the tool-call ledger, leases, PG cancellation, Docker isolation, the configured OpenAI-compatible model, policy, production Verifier, heartbeat, and pg-boss recovery as one unit. Runtime startup is awaitable and does not expose HTTP until queue migration, queue registration, and the first durable scan job succeed. Its `start`/`stop` lifecycle is idempotent, stops recovery before consumers, attempts every cleanup after failures, and waits for asynchronous UNLISTEN before closing database resources. Startup requires immutable runtime and verification image digests; each Run workspace is resolved below the registered worktree root by the environment factory.
- `createProductionVerifier` now resolves an administrator-reviewed minimum plan, validates non-empty structured argv commands and unique evidence names, and executes every required command in an independent restricted RunEnvironment. Its non-removable Diff Safety authority runs on the host only after revalidating the Run ID, canonical worktree root, top-level, and common Git directory; tracked and untracked files are checked without mounting Git metadata into either container. Each user acceptance criterion must be covered exactly by at least one required check; missing coverage is `inconclusive`, while an unsafe diff or nonzero required check is `failed`. Plan loading, container startup, command execution, diff inspection, and cleanup failures are converted to stable redacted `inconclusive` evidence rather than leaked exceptions.
- `createProjectYamlVerificationPlanProvider` now loads each reviewed source baseline's `.ai-agent/project.yaml` during Worker startup, validates a strict version-1 schema of named argv commands and criterion coverage, and caches mutation-safe plan copies by project identity. The administrator-owned project registry canonicalizes configs, sources, and worktree roots, rejects duplicate/aliased/cross-boundary entries, and composes a separate plan provider, runtime factory, Diff Safety checker, and change reader per project. The legacy `LECODING_PROJECT_ID` tuple remains the single-project fallback; the explicit reviewed `"*"` coverage marker supports future task criteria without deriving commands from model or user text.
- Verification cancellation now shares the Run handle's AbortSignal: a local or cross-worker cancellation broadcast interrupts the active required check, prevents later checks from starting, emits stable inconclusive cancellation evidence, and still disposes the independent verification container.
- A project-specific offline verification image now pins its base and pnpm version, installs the reviewed lockfile during image build, hoists workspace package links into the root dependency volume, and runs checks with network disabled. Worker startup requires a separate immutable `LECODING_VERIFICATION_IMAGE`; the worktree is mounted below `/workspace`, image dependencies are restored through a nested anonymous volume, and `docker rm -v` removes that volume after every verification. A live Docker Desktop build plus a clean Git worktree run proved workspace package resolution, focused tests, and all typecheck tasks under the production-equivalent non-root/read-only/network-none/resource-bounded contract.
- Run environments are now routed per Run instead of binding one fixed workspace to the Worker process. The runtime factory creates `<worktreeRoot>/<runId>`, starts a Docker environment scoped to that canonical path, and routes later perform/inspect/dispose calls to the matching implementation. If container startup fails, the worktree remains durable while RunEngine parks the Run offline. Recovery safely reopens it only after Git proves its top-level and common Git directory belong to the registered source repository, preserving uncommitted Run changes without accepting an arbitrary pre-existing directory. Git status, rather than `docker diff`, is the changed-file authority because Docker layer inspection cannot observe bind-mounted source changes.
- Successful and failed Runs now dispose the runtime container and anonymous dependency volume before their terminal transition, while passing `keep` through the routed environment so only the managed Git worktree remains available to the changes/evidence API. Cleanup failure adds stable inconclusive verification evidence and can no longer leave a silent `sleep infinity` container behind.
- A browser-driven Run from clean committed baseline `b889586` reached `succeeded` through real PostgreSQL, the configured compatible model, manual approval, Docker runtime, independent verification image, durable SSE, and host Diff Safety. Tests, typecheck, Diff Safety, and the acceptance criterion all passed; terminal cleanup left only PostgreSQL running. Full evidence is recorded in [`evidence/browser-e2e-2026-08-26.md`](evidence/browser-e2e-2026-08-26.md).
- The Worker serves a same-origin `/api/v1` control plane and prebuilt Web console. Loopback remains available without authentication for local development; a configured Bearer token protects every API/SSE/command route, and non-loopback binding additionally requires an explicit TLS-proxy assertion. The Web login shell keeps its token in tab-scoped session storage and the SDK sends it only in Authorization headers. Bootstrap exposes only registered project IDs; the selector aborts and clears the old Run/SSE/evidence scope before loading the chosen project's bounded history. A separate `RunHistory` read model queries at most 50 newest snapshots for the selected registered project and excludes continuation/tool-result internals. A project-routed read-only Git change seam derives and revalidates the managed worktree, then returns at most 500 paths and a bounded unified Diff including untracked files. The UI lists the latest 20 Runs, restores the newest after refresh, safely switches SSE ownership, displays files/Diff as text, reconnects from the last accepted sequence without re-rendering at-least-once duplicates, replays submitted/delivered/question conversation events through the terminal status event, renders verification checks, cancels non-terminal Runs, resolves a displayed pending approval as approve-once or reject-once, answers a displayed persisted model question, and appends constraints during active Run states. Live steering bypasses lease contention through an ordered PostgreSQL mailbox, is staged durably at model boundaries, and is forwarded by both Responses and Chat Completions adapters without interrupting an in-flight tool action. The API rejects Run-wide approval scope, stale question IDs, and messages above 4,000 characters. A real HTTP integration test covers unauthenticated rejection plus the public Client SDK create/SSE/inspect/cancel loop.
- Type checking passes across all implemented packages.

## Current automated baseline

```text
Test files: 52 passed, 1 opt-in live baseline file skipped
Tests:      229 passed, 1 test skipped in the latest host run
Typecheck:  12 package tasks passed
Build:      Web production bundle passed
```

## Phase 0 evidence exception

- On 2026-08-26 the project owner accepted Phase 0 as complete for sequencing purposes because the current Darwin environment cannot produce target-Linux evidence. The evidence debt remains explicit: run `node scripts/run-target-linux-isolation.mjs <report-path>` on the actual target Linux Worker host and commit its generated report before making a production isolation claim.

## Environment note

Docker Desktop 27.5.1 previously ran all seven Docker environment tests successfully. The latest sandboxed full-suite run may skip daemon-backed cases when socket access is unavailable; deterministic Docker-plan tests still run. Docker Desktop on macOS and the five-task baseline are development evidence only, and no target-Linux isolation claim is considered verified yet. `run-target-linux-isolation.mjs` was exercised on this Darwin host and correctly refused to run before contacting Docker or writing a report. The configured third-party Chat Completions provider has passed both a two-turn protocol smoke test and the five-task quality baseline. A loopback-bound local PostgreSQL container previously passed real Worker composition/start/stop and the original six-table schema initialization; the steering-mailbox table and pg-boss schema have PostgreSQL-compatible PGlite integration evidence but still need inclusion in the next real-service smoke. This does not replace target-service evidence.

The Worker now has an executable deployment host that creates a bounded real node-postgres Pool plus a replaceable dedicated LISTEN client, proves readiness before accepting work, and performs idempotent SIGINT/SIGTERM shutdown without logging the connection URI. It accepts a canonical administrator-owned multi-project registry with a legacy one-project fallback and creates a project-routed managed worktree, runtime, verifier, and Diff reader per Run. Target-service PostgreSQL smoke evidence remains separate from the completed local-container smoke. Verification commands receive immediate Run cancellation and execute from a separate dependency-prepared immutable image; deployment automation must rebuild and publish that image whenever a reviewed lockfile or workspace manifest changes. Loopback remains optionally unauthenticated; remote binding requires the implemented Bearer-token and TLS-proxy assertion controls.
